---
- name: converge
  hosts: all
  become: yes
  gather_facts: yes

  roles:
    - role: ansible-role-luks
      luks_devices:
        - device: /dev/loop0
          name: luks-disk
          keyfile: /etc/luks_keyfile
